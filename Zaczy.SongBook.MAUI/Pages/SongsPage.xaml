<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Zaczy.SongBook.MAUI.ViewModels"
             x:Class="Zaczy.SongBook.MAUI.Pages.SongsPage"
             Title="Piosenki"
             >

    <ContentPage.ToolbarItems>
        <!-- Menu / Toolbar item that triggers import from API -->
        <ToolbarItem Text="Import" Command="{Binding FetchCommand}" Order="Primary" Priority="0" />
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <Grid RowDefinitions="Auto,Auto,*,Auto" Padding="12">
            <!-- Filters -->
            <StackLayout Orientation="Horizontal" Spacing="8" Grid.Row="0">
                <Entry Placeholder="Tytuł lub wykonawca"
                       HorizontalOptions="FillAndExpand"
                       Text="{Binding TitleFilter}"
                       Completed="OnTitleFilterCompleted" />
            </StackLayout>

            <StackLayout Orientation="Horizontal" Spacing="8" Grid.Row="1" Margin="0,8,0,12">
                <Button Text="Szukaj" Command="{Binding FilterCommand}" HorizontalOptions="Start" />
                <Button Text="Wyczyść" Command="{Binding ClearCommand}" HorizontalOptions="Start" />
                <Button Text="Odśwież" Command="{Binding LoadCommand}" HorizontalOptions="Start" />
                <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}"  />
            </StackLayout>

            <!-- List -->
            <CollectionView ItemsSource="{Binding Songs}" SelectionMode="Single" Grid.Row="2" x:Name="SongsCollection">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <!-- Replace Border with a Grid that draws only top and bottom lines -->
                        <Grid Margin="0,0" Padding="0">
                          <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />  <!-- content -->
                            <RowDefinition Height="1" />     <!-- bottom line -->
                          </Grid.RowDefinitions>

                          <!-- content -->
                          <StackLayout Grid.Row="0" Padding="0,5,0,5">
                            <Label Text="{Binding Title}" FontAttributes="Bold" LineBreakMode="TailTruncation" />
                            <Label Text="{Binding Artist}" FontSize="Small" TextColor="Gray" />
                          </StackLayout>

                          <!-- bottom line -->
                          <BoxView Grid.Row="1" BackgroundColor="#ddd" HorizontalOptions="Fill" />

                          <!-- Tap handler attached to the Grid -->
                          <Grid.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnItemTapped" NumberOfTapsRequired="1" CommandParameter="{Binding .}" />
                          </Grid.GestureRecognizers>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Label Grid.Row="3" Text="Tap item to view details" HorizontalOptions="Center" TextColor="Gray" FontSize="Small" Margin="0,8,0,0" />
        </Grid>
    </ContentPage.Content>
</ContentPage>