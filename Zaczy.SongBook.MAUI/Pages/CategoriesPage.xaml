<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:data="clr-namespace:Zaczy.SongBook.Data;assembly=Zaczy.SongBook"
    xmlns:pages="clr-namespace:Zaczy.SongBook.MAUI.Pages;assembly=Zaczy.SongBook.MAUI"
    xmlns:mi="http://www.aathifmahir.com/dotnet/2022/maui/icons"
    x:Class="Zaczy.SongBook.MAUI.Pages.CategoriesPage"
    x:Name="Root"
    x:DataType="pages:CategoriesPage"
    Title="Kategorie"
    >

    <ContentPage.ToolbarItems>
        <!-- ToolbarItem does not inherit the page BindingContext, use x:Reference to the page -->
        <ToolbarItem 
            Text="Odśwież"
            BindingContext="{x:Reference Root}"
            Command="{Binding FetchCategoriesCommand}"
            Order="Primary"
            Priority="0" />
		<ToolbarItem
            BindingContext="{x:Reference Root}"
            Command="{Binding FetchCategoriesCommand}"
            Order="Primary"
			IconImageSource="sync2_ico.png"
            Priority="0" />
	</ContentPage.ToolbarItems>

    <Grid Padding="12" RowDefinitions="Auto,Auto,*,Auto">

        <Label Grid.Row="0" Padding="10,20,10,30">
Poniżej widoczne są katalogi piosenek pobrane z serwera aplikacji.
	 
Wybierz jeden lub więcej katalogów, aby pobrać piosenki wchodzące w ich skład.
        </Label>
	  
        <!-- Empty state message -->
        <Label Grid.Row="1"
               Text="W tej chwili nie ma żadnych kategorii. Wybierz opcję Pobierz, żeby pobrać z serwera"
               HorizontalOptions="Center"
               VerticalOptions="Center"
               HorizontalTextAlignment="Center"
               Margin="20"
               FontSize="14"
               TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}"
               IsVisible="{Binding IsEmptyCategories}" />

        <!-- CollectionView must be in its own Grid row so it receives gestures correctly -->
        <CollectionView 
            x:Name="CategoriesCollection"
            Grid.Row="2"
            ItemsSource="{Binding Categories}"
            SelectionChanged="CategoriesCollection_SelectionChanged"
            SelectionMode="None"
            >

            <CollectionView.ItemsLayout>
                <GridItemsLayout Orientation="Vertical"
                   Span="2"
                   HorizontalItemSpacing="8"
                   VerticalItemSpacing="8" />
            </CollectionView.ItemsLayout>

            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="data:SongCategoryEntity">
                    <Border 
                        Margin="0" 
                        Padding="0"
                        BackgroundColor="{Binding CategoryColorThemed}"
                        Stroke="{Binding CategoryColorThemed}"
                        StrokeThickness="2"
                        >
                        <Border.Triggers>
                            <DataTrigger TargetType="Border" Binding="{Binding IsSelected}" Value="True">
                                <Setter Property="Stroke" Value="{AppThemeBinding Light=#A64640, Dark=#E68680}" />
                            </DataTrigger>
                        </Border.Triggers>
                        
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="Selected">
                                    <VisualState.Setters>
                                        <Setter Property="StrokeThickness" Value="2" />
                                        <Setter Property="Stroke" Value="{AppThemeBinding Light=#A64640, Dark=#E68680}" />
                                        <Setter TargetName="TitleLabel" Property="Label.TextColor" Value="{AppThemeBinding Light=#A64640, Dark=#E68680}" />
                                        <Setter TargetName="CountLabel" Property="Label.TextColor" Value="{AppThemeBinding Light=#A64640, Dark=#E68680}" />
                                        <Setter TargetName="DescriptionLabel" Property="Label.TextColor" Value="{AppThemeBinding Light=#A64640, Dark=#E68680}" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>

                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="4" />
                        </Border.StrokeShape>

                        <!-- Attach Tap to the Border; make the decorative BoxView InputTransparent so it doesn't intercept touches -->
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnTileTapped" NumberOfTapsRequired="1" />
                        </Border.GestureRecognizers>

                        <Grid RowDefinitions="Auto,Auto,Auto" ColumnDefinitions="*,Auto">
                            <BoxView 
                                Grid.Row="0"
                                Grid.Column="0"
                                BackgroundColor="{Binding CategoryColorThemed}"
                                Grid.ColumnSpan="2"
                                InputTransparent="True"  
                                />

                            <HorizontalStackLayout 
                                   Grid.Column="0"
                                   Margin="10,10,20,10"
                                   Grid.Row="0"
                                >
                                <Label x:Name="TitleLabel"
                                       Text="{Binding Name}"
                                       FontAttributes="Bold"
                                       FontSize="{OnIdiom 12, Tablet=16}"
                                       VerticalOptions="Center">
                                    <!--<Label.Triggers>
                                        <DataTrigger TargetType="Label" Binding="{Binding IsSelected}" Value="True">
                                            <Setter Property="TextColor" Value="{AppThemeBinding Light=#A64640, Dark=#E68680}" />

                                        </DataTrigger>
                                    </Label.Triggers>-->
                                </Label>

                                <Image x:Name="PrivateIcon"
                                       Grid.Column="1"
                                       Grid.Row="2"
                                       Source="{mi:Fluent LockOpen16, IconSize=12, IconColor={AppThemeBinding Dark={StaticResource PrimaryDark}, Light={StaticResource Primary}}}"
                                       WidthRequest="16"
                                       HeightRequest="16"
                                       Margin="3,0,5,0"
                                       Opacity="0.7"
                                       VerticalOptions="End"
                                       HorizontalOptions="End"
                                       IsVisible="{Binding IsPrivate}"
                                       />
                            </HorizontalStackLayout>

                            <Image x:Name="SelectedIcon"
                                   Grid.Column="1"
                                   Grid.Row="1"
                                   Source="{mi:Fluent Checkmark24, IconSize=12, IconColor={AppThemeBinding Dark={StaticResource White}, Light={StaticResource White}}}"
                                   WidthRequest="32"
                                   HeightRequest="32"
                                   Margin="0,0,5,0"
                                   Opacity="0.6"
                                   VerticalOptions="Start"
                                   HorizontalOptions="End"
                                   IsVisible="{Binding IsSelected}"
                                   />

                            <Label x:Name="CountLabel"
                                   Grid.Column="0"
                                   Grid.Row="1"
                                   Text="{Binding SongsCount, StringFormat='Piosenek: {0}'}"
                                   Margin="10,10,0,10"
                                   FontSize="10"
                                   >
                                <!--<Label.Triggers>
                                    <DataTrigger TargetType="Label" Binding="{Binding IsSelected}" Value="True">
                                        <Setter Property="TextColor" Value="{AppThemeBinding Light=#A64640, Dark=#E68680}" />

                                    </DataTrigger>
                                </Label.Triggers>-->
                            </Label>

                            <Label x:Name="DescriptionLabel"
                                   Grid.Column="0"
                                   Grid.Row="2"
                                   Grid.ColumnSpan="2"
                                   Text="{Binding Description}"
                                   FontAttributes="Bold"
                                   FontSize="{OnIdiom 10, Tablet=14}"
    							   Opacity="0.5"
    							   Padding="10,15,5,20"
                                   VerticalOptions="Center">
                                <Label.Triggers>
                                    <DataTrigger TargetType="Label" Binding="{Binding IsSelected}" Value="True">
                                        <Setter Property="TextColor" Value="{AppThemeBinding Light=#A64640, Dark=#E68680}" />
                                    </DataTrigger>
                                </Label.Triggers>

                            </Label>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </Grid>
</ContentPage>